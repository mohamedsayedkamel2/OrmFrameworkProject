2- Make the library check if an object exisists in the db and if so then make the library check if that object can be updated and if that is true then it will be updated if the object can't be updated then it won't be updated and an error will be thrown